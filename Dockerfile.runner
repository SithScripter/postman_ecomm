FROM node:18-alpine

# newman + allure/htmlextra reporters
RUN npm install -g newman newman-reporter-allure newman-reporter-htmlextra

# where we work & keep the collection + test asset
WORKDIR /etc/newman
COPY E2E_Ecommerce.postman_collection.json .
COPY headerimage@2x.jpg .
